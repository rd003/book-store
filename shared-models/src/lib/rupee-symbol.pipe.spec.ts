import { RupeeSymbolPipe } from './rupee-symbol.pipe';

describe('RupeeSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new RupeeSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
